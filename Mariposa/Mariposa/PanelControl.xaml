<Page x:Class="Mariposa.ControlPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="ControlPanel">

    <StackPanel Margin="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

        <TextBlock Text="Posición inicial"
                   FontWeight="Bold"
                   Margin="0,0,0,5"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Label Content="X:"
                   VerticalAlignment="Center"
                   Margin="5"/>
            <TextBox Width="40"
                     Margin="5"/>
            <Label Content="Y:"
                   VerticalAlignment="Center"
                   Margin="5"/>
            <TextBox Width="40"
                     Margin="5"/>
        </StackPanel>

        <!-- Botones de dirección -->
        <Grid Margin="10"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="↑"
                    Grid.Row="0"
                    Grid.Column="1"
                    Width="40"
                    Height="40"
                    Margin="2"/>
            <Button Content="←"
                    Grid.Row="1"
                    Grid.Column="0"
                    Width="40"
                    Height="40"
                    Margin="2"/>
            <Button Content="→"
                    Grid.Row="1"
                    Grid.Column="2"
                    Width="40"
                    Height="40"
                    Margin="2"/>
            <Button Content="↓"
                    Grid.Row="2"
                    Grid.Column="1"
                    Width="40"
                    Height="40"
                    Margin="2"/>
        </Grid>

        <TextBlock Text="N° saltos:"
                   Margin="10,10,10,2"
                   HorizontalAlignment="Center"/>
        <TextBox Width="100"
                 Margin="10,0,0,0"
                 HorizontalAlignment="Center"/>
    </StackPanel>
</Page>
